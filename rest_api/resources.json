{
  "tree": {
    "name": "tree",
    "options": {
      "model": "tree",
      "read_only": false,
      "accepted_filters": ["pge_pmd_num", "division", "circuit_name", "region", "project"],
      "excluded_fields": ["__v"]
    }
  },
  "pmd": {
    "name": "pmd",
    "options": {
      "model": "pmd",
      "read_only": true,
      "accepted_filters": ["pge_pmd_num", "division", "project"],
      "excluded_fields": ["__v"]
    }
  },
  "line": {
    "name": "circuit",
    "options": {
      "model": "circuit",
      "read_only": true,
      "accepted_filters": ["project", "name"]
      "excluded_fields": ["__v"]
    }
  },
  "user": {
    "name": "user",
    "options": {
      "model": "cufs",
      "read_only": true,
      "accepted_filters": ["uniq_id", "scuf"],
      "excluded_fields": ["__v", "workorder", "project", "assigned_workorders", "division_data", "scheduled_workorders", "scheduled_data", "password", "checksum"]
    }
  },
  "mapfeatures": {
    "name": "mapfeatures",
    "options": {
      "model": "mapfeatures",
      "read_only": true,
      "excluded_fields": ["__v"]
    }
  },
  "divisions": {
    "name" : "divisions",
    "options" : {
      "model" : "divisions",
      "read_only" : true,
      "accepted_filters" : ["project", "division", "pge_pmd_num" ],
      "excluded_fields": ["__v"]
    }
  },
  "mapAnnotations": {
    "name": "mapannotations",
    "options": {
      "model": "map_annotations",
      "read_only": true,
      "accepted_filters": ["tree_id", "deleted", "esri_graphics"]
    }
  }
}
